<nav class="bg-white shadow" data-controller="mobile-menu">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex">
        <!-- Logo -->
        <div class="flex-shrink-0 flex items-center">
          <%= link_to root_path, class: "text-xl font-bold text-blue-600 flex items-center space-x-2" do %>
            <iconify-icon icon="majesticons:library" class="h-8 w-8 flex-shrink-0"></iconify-icon>
            <span>Library Management</span>
          <% end %>
        </div>

        <!-- Navigation Links -->
        <% if user_signed_in? %>
          <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
            <%= link_to dashboard_path, class: "#{current_page?(dashboard_path) ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'} flex items-center space-x-2 px-1 pt-1 border-b-2 text-sm font-medium" do %>
              <iconify-icon icon="heroicons:chart-bar" class="h-6 w-6 flex-shrink-0"></iconify-icon>
              <span>Dashboard</span>
            <% end %>

            <%= link_to books_path, class: "#{current_page?(books_path) ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'} flex items-center space-x-2 px-1 pt-1 border-b-2 text-sm font-medium" do %>
              <iconify-icon icon="heroicons:book-open" class="h-6 w-6 flex-shrink-0"></iconify-icon>
              <span>Books</span>
            <% end %>

            <%= link_to members_path, class: "#{current_page?(members_path) ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'} flex items-center space-x-2 px-1 pt-1 border-b-2 text-sm font-medium" do %>
              <iconify-icon icon="heroicons:users" class="h-6 w-6 flex-shrink-0"></iconify-icon>
              <span>Members</span>
            <% end %>

            <%= link_to authors_path, class: "#{current_page?(authors_path) ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'} flex items-center space-x-2 px-1 pt-1 border-b-2 text-sm font-medium" do %>
              <iconify-icon icon="heroicons:pencil" class="h-6 w-6 flex-shrink-0"></iconify-icon>
              <span>Authors</span>
            <% end %>

            <%= link_to categories_path, class: "#{current_page?(categories_path) ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'} flex items-center space-x-2 px-1 pt-1 border-b-2 text-sm font-medium" do %>
              <iconify-icon icon="heroicons:tag" class="h-6 w-6 flex-shrink-0"></iconify-icon>
              <span>Categories</span>
            <% end %>
          </div>
        <% end %>
      </div>

      <!-- User menu -->
      <div class="hidden sm:ml-6 sm:flex sm:items-center">
        <% if user_signed_in? %>
          <%= button_to destroy_user_session_path, method: :delete, class: "text-gray-500 hover:text-gray-700 px-3 py-2 rounded-md text-sm font-medium flex items-center space-x-2" do %>
            <iconify-icon icon="heroicons:arrow-left-on-rectangle" class="h-6 w-6 flex-shrink-0"></iconify-icon>
            <span>Sign out</span>
          <% end %>
        <% end %>
      </div>

      <!-- Mobile menu button -->
      <div class="-mr-2 flex items-center sm:hidden">
        <button type="button" 
                data-action="click->mobile-menu#toggle"
                class="inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-gray-500 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-blue-500" 
                aria-controls="mobile-menu" 
                aria-expanded="false">
          <span class="sr-only">Open main menu</span>
          <!-- Menu open: "hidden", Menu closed: "block" -->
          <iconify-icon data-mobile-menu-target="openButton" icon="heroicons:bars-3" class="block h-6 w-6"></iconify-icon>
          <!-- Menu open: "block", Menu closed: "hidden" -->
          <iconify-icon data-mobile-menu-target="closeButton" icon="heroicons:x-mark" class="hidden h-6 w-6"></iconify-icon>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile menu -->
  <div class="hidden sm:hidden" id="mobile-menu" data-mobile-menu-target="menu">
    <% if user_signed_in? %>
      <div class="pt-2 pb-3 space-y-1">
        <%= link_to dashboard_path, class: "#{current_page?(dashboard_path) ? 'bg-blue-50 border-blue-500 text-blue-700' : 'border-transparent text-gray-500 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-700'} flex items-center space-x-2 pl-3 pr-4 py-2 border-l-4 text-base font-medium" do %>
          <iconify-icon icon="heroicons:chart-bar" class="h-6 w-6 flex-shrink-0"></iconify-icon>
          <span>Dashboard</span>
        <% end %>

        <%= link_to books_path, class: "#{current_page?(books_path) ? 'bg-blue-50 border-blue-500 text-blue-700' : 'border-transparent text-gray-500 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-700'} flex items-center space-x-2 pl-3 pr-4 py-2 border-l-4 text-base font-medium" do %>
          <iconify-icon icon="heroicons:book-open" class="h-6 w-6 flex-shrink-0"></iconify-icon>
          <span>Books</span>
        <% end %>

        <%= link_to members_path, class: "#{current_page?(members_path) ? 'bg-blue-50 border-blue-500 text-blue-700' : 'border-transparent text-gray-500 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-700'} flex items-center space-x-2 pl-3 pr-4 py-2 border-l-4 text-base font-medium" do %>
          <iconify-icon icon="heroicons:users" class="h-6 w-6 flex-shrink-0"></iconify-icon>
          <span>Members</span>
        <% end %>

        <%= link_to authors_path, class: "#{current_page?(authors_path) ? 'bg-blue-50 border-blue-500 text-blue-700' : 'border-transparent text-gray-500 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-700'} flex items-center space-x-2 pl-3 pr-4 py-2 border-l-4 text-base font-medium" do %>
          <iconify-icon icon="heroicons:pencil" class="h-6 w-6 flex-shrink-0"></iconify-icon>
          <span>Authors</span>
        <% end %>

        <%= link_to categories_path, class: "#{current_page?(categories_path) ? 'bg-blue-50 border-blue-500 text-blue-700' : 'border-transparent text-gray-500 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-700'} flex items-center space-x-2 pl-3 pr-4 py-2 border-l-4 text-base font-medium" do %>
          <iconify-icon icon="heroicons:tag" class="h-6 w-6 flex-shrink-0"></iconify-icon>
          <span>Categories</span>
        <% end %>

        <%= button_to destroy_user_session_path, method: :delete, class: "w-full text-left border-transparent text-gray-500 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-700 flex items-center space-x-2 pl-3 pr-4 py-2 border-l-4 text-base font-medium" do %>
          <iconify-icon icon="heroicons:arrow-left-on-rectangle" class="h-5 w-5 mr-1"></iconify-icon>
          <span>Sign out</span>
        <% end %>
      </div>
    <% end %>
  </div>
</nav>